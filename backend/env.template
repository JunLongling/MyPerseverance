# INFRASTRUCTURE & BACKEND CONFIGURATION (.env.example)
# -----------------------------------------------------------
# To run this project locally, copy this file to 'backend/.env'.
# These default values are pre-configured to work out-of-the-box with the
# local docker-compose setup. You do not need to change them for local development.

# PostgreSQL Database Credentials
# The official PostgreSQL image will automatically create this database and user.
POSTGRES_DB=myperseverance
POSTG-RES_USER=postgres
POSTGRES_PASSWORD=postgres

# JWT Secret for signing tokens.
# IMPORTANT: This default value is for local development convenience ONLY.
# For any real deployment, generate a new secure key using a command like: openssl rand -hex 32
JWT_SECRET=REMOVED_JWT_SECRET=

# The origin URL of the client application (your frontend).
# This is used for the backend's CORS (Cross-Origin Resource Sharing) configuration.
# It must match the port your frontend development server is running on.
CLIENT_ORIGIN_URL=http://localhost:5173